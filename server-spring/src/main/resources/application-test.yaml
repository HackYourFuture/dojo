spring:
  application:
    name: dojo-server-test
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

# API Docs
scalar:
  enabled: true

# Health and metrics
management:
  endpoint:
    health:
      show-details: when_authorized